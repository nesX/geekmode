

## 🎯 Siguiente: CRUD Admin de Productos

### Backend — 6 endpoints nuevos en `admin.routes.js`

Todos protegidos con tu middleware JWT existente.

**Repositorio — `repositories/product.repository.js`** (ampliar el que ya tienes):

| Función | SQL |
|---|---|
| `findAll()` | Como `findAllActive()` pero sin filtro `is_active`, para ver todo en el admin |
| `create(data)` | `INSERT INTO products` retornando el producto creado |
| `update(id, data)` | `UPDATE products SET ... WHERE id = $1 RETURNING *` |
| `deactivate(id)` | `UPDATE products SET is_active = false` — nunca `DELETE` |
| `createVariant(data)` | `INSERT INTO variants` |
| `updateVariantStock(id, stock)` | `UPDATE variants SET stock = $2 WHERE id = $1` |

**Rutas:**

```
GET    /api/admin/products              → listar todos (activos + inactivos)
POST   /api/admin/products              → crear producto
PUT    /api/admin/products/:id          → editar producto
DELETE /api/admin/products/:id          → desactivar (soft delete)
POST   /api/admin/products/:id/variants → agregar variante
PATCH  /api/admin/variants/:id/stock    → ajustar stock
```

**Validación Zod** para el body de crear/editar:

```js
const ProductSchema = z.object({
  name: z.string().min(3),
  slug: z.string().min(3),        // auto-generado desde name en el service
  description: z.string().optional(),
  base_price: z.number().positive(),
  image_url: z.string().url().optional()
})
```

---

### Frontend Admin — `AdminApp.jsx`

Tres vistas dentro del SPA:

**Vista 1 — Tabla de productos** (la principal)
- Lista todos los productos con columnas: imagen, nombre, precio, stock total, estado (activo/inactivo)
- Botón "Nuevo producto"
- Por cada fila: botones Editar, Ver variantes, Desactivar

**Vista 2 — Modal crear/editar producto**
- Campos: nombre, precio, descripción
- El slug se auto-genera desde el nombre (en el service del backend)
- Upload de imagen a Cloudinary (ya tienes `upload.middleware.js`)

**Vista 3 — Modal de variantes**
- Sub-tabla con las variantes del producto seleccionado (talla, color, stock)
- Formulario inline para agregar nueva variante
- Input de stock editable por variante

---

### Orden de ejecución

```
1. Ampliar product.repository.js con las funciones de escritura
2. Ampliar product.service.js con la lógica de admin
3. product.controller.js — handlers admin
4. admin.routes.js — registrar endpoints con middleware JWT
5. AdminApp.jsx — tabla + modales
```


### Done!

  Backend - 6 endpoints CRUD Admin                                                                                                                                                                                                                            
  ┌────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐                                
  │                      Archivo                       │                                                                               Cambios                                                                               │                                
  ├────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤                                
  │ server/src/repositories/product.repository.js      │ +7 funciones: findAll, create, update, deactivate, createVariant, updateVariantStock, findVariantsByProductId                                                       │                                
  ├────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤                                
  │ server/src/services/product.service.js             │ +7 funciones admin: getAllProductsAdmin, createProduct (con slugify auto), updateProduct, deactivateProduct, addVariant, updateVariantStock, getVariantsByProductId │                                
  ├────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤                                
  │ server/src/controllers/admin.product.controller.js │ Nuevo - 6 handlers con validacion Zod (ProductSchema, VariantSchema, StockSchema)                                                                                   │                                
  ├────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ server/src/routes/admin.routes.js                  │ +6 rutas protegidas con requireAdmin                                                                                                                                │
  └────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
  Endpoints registrados:

  GET    /api/admin/products              → listar todos (activos + inactivos)
  POST   /api/admin/products              → crear producto (Zod validated)
  PUT    /api/admin/products/:id          → editar producto (Zod validated)
  DELETE /api/admin/products/:id          → soft delete (is_active = false)
  POST   /api/admin/products/:id/variants → agregar variante
  PATCH  /api/admin/variants/:id/stock    → ajustar stock

  Frontend Admin - 3 componentes
  ┌────────────────────────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │                    Archivo                     │                                                         Que hace                                                          │
  ├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ client/src/components/admin/AdminDashboard.jsx │ Modificado - Ahora tiene navegacion funcional entre vistas (Dashboard, Productos, etc.)                                   │
  ├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ client/src/components/admin/ProductsTable.jsx  │ Nuevo - Tabla con imagen, nombre, precio, stock, estado, y botones Editar/Variantes/Desactivar                            │
  ├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ client/src/components/admin/ProductModal.jsx   │ Nuevo - Modal crear/editar con campos nombre, precio, descripcion, URL imagen. Slug se auto-genera en el backend          │
  ├────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ client/src/components/admin/VariantsModal.jsx  │ Nuevo - Sub-tabla de variantes con stock editable inline y formulario para agregar nuevas variantes (talla, color, stock) │
  └────────────────────────────────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

